[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pygit"
version = "0.1.0"
description = "A Git clone written in Python"
requires-python = ">=3.9"

[project.scripts]
pygit = "cli:main"

[tool.setuptools]
py-modules = ["cli", "repository", "models", "main"]