# hash table - is a fundamental data structure in computer science
# take a key("string") convert it is integer index .
# goal: Distribute keys uniformly across the table to minimize collisions.
# index - hash(key ) % table size.
#Buckets/Slots:An array where each element is a "bucket" that stores one or more key-value pairs.
#Collision:
#Occurs when two different keys produce the same hash index.
# Must be handled explicitly.



